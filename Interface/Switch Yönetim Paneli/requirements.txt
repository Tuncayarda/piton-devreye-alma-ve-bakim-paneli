# Her platformda gereken ortak bağımlılıklar.
# Pencere katmanı platforma göre değişir — kendi dosyanı kur:
#   Windows : pip install -r requirements-windows.txt
#   macOS   : pip install -r requirements-macos.txt
#   Ubuntu  : pip install -r requirements-linux.txt
# Paket üretmek için ayrıca: requirements-build.txt
requests>=2.32,<3
pywebview>=5.3,<6
