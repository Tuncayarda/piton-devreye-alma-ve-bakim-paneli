# Windows
# Pencere motoru: WebView2 (Edge Chromium).
#
# WebView2 çalışma zamanı bir SİSTEM bileşenidir, pip ile gelmez.
# Windows 10 21H2 ve sonrasında işletim sistemiyle birlikte kuruludur.
# Yoksa Microsoft'un ücretsiz "Evergreen WebView2 Runtime" kurulumu gerekir:
#   https://developer.microsoft.com/microsoft-edge/webview2/
# Eksikse uygulama açılışta bunu anlaşılır bir pencereyle bildirir.
-r requirements.txt
pythonnet>=3.0,<4        # pywebview'in WinForms/WebView2 köprüsü
